A element contains one or more tags defining a single accessable feature of a
device.  Gui interfaces use feature tags to create a single element.

Child of:
	Device

May Contain:
	A list of elements
